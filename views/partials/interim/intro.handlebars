<section id="promises" class="{{ role }}">
    <div class="container">
        <h2 class="section-title">{{ translation.choice.INTERIM }}</h2>
        <div class="section-container">
            <p>Hier komt tekst gericht op leveraar van interimmers</p>
        </div>
    </div>
</section>